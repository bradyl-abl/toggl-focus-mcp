mcp[cli]>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Kept in step with the dependencies table in pyproject.toml, which is the
# source of truth. Prefer `pip install -e .` over installing from this file.
